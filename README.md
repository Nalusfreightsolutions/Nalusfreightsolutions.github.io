# Jarelys Command Center — Project Handoff Notes

This folder is everything you need to keep working on this app in Claude Code.

## Files
- `index.html` — the full app (single-file HTML/CSS/JS). This is the latest version, with the Pit Map & Routing tab and a save-reliability fix already applied.
- `supabase_schema.sql` — the Postgres schema + Row Level Security policies for the Supabase migration (not yet wired into `index.html` — see "Next steps" below).

## What this app is
A business management tool for two trucking/aggregate companies, Nalu Nui Logistics (NNL) and Nalu's Freight Solutions (NFS): loads/tickets, driver compliance, expenses, invoicing, quotes, sales jobs, annual taxes, financial reports, and (new) a pit map with routing and a rate calculator.

## What just changed (this session)
1. **Pit Map & Routing tab** — added. Uses free services only:
   - Leaflet + OpenStreetMap for the map (no key)
   - Nominatim for geocoding pit/job addresses (no key, keep usage light)
   - OpenRouteService for driving distance/time (free API key required — user needs to sign up at openrouteservice.org, get a token from the Dashboard → Tokens, and paste it into the app)
   - Includes a rate calculator: target day-rate → $/load, $/ton or $/yard, $/hour, $/mile, with a hard-coded $2/mile minimum warning.
2. **Save reliability fix** — the app was built to save via `window.storage`, an API that only exists inside the Claude.ai artifact preview. Outside that environment (e.g. this file opened directly, or served locally), that API doesn't exist and saves could silently fail. Added a `storageAdapter` that falls back to `localStorage` automatically when `window.storage` isn't present, so saving now works correctly when running this file anywhere, including from Claude Code / a local server.
3. **Visible error handling** — `render()` now catches exceptions and shows a red banner in the UI instead of failing silently, so future bugs are easier to spot and report instead of just "nothing happens."

## What just changed (session 2)
1. **"Clients" → "Contacts" rename** — within the Clients & Pits tab only: subtab button, page headings ("Add/Edit Contact", "All Contacts"), and empty-state text. The nested per-contact people list (name/email/job title) is now labeled "Contact People" to avoid clashing with the new top-level "Contacts" label. Other tabs (Quotes, Sales Jobs, Invoices) still say "Client" — that wasn't in scope.
2. **NFS Sales Team tab order** — now Pit Map & Routing → Quotes → Landed Jobs (was Quotes → Landed Jobs → Pit Map & Routing).
3. **OpenRouteService API key baked in as a default** — `state.orsApiKey` now defaults to the user's real key instead of `''`, and `loadState()` re-applies that default if a previously-saved blank key would otherwise override it. The routing tab should never prompt for a key again; "Change key" still works if it ever needs to be swapped.
4. **B&B Pits data fixed** — the three B&B pit records (Bithlo, Clermont, St. Cloud) were mislabeled `"B&B Pits (C.C. Calhoun, Inc.)"` and had their pricing filed under `dumpFees` instead of `materials`. Renamed to `"B&B Pits"` (no Calhoun affiliation — they're unrelated companies) and moved their pricing into `materials`, so it now shows under "Materials & Pricing" in the Pits table instead of "Dump Fees".
5. **Pit Map material search** — "Material Needed" on "Find Nearby Pits for a Job" is now a type-ahead text input (`<input list>`/datalist) instead of a giant `<select>` with every material in the system. Typing filters suggestions live; leaving it blank still means "all materials."
6. **Quotes: filtering, printing, monthly report** — added to the Quotes subtab:
   - Filters on the All Quotes table: Status, Client, Submitted By, Date From/To (all combos, "Clear Filters" resets them).
   - A **Print** button per quote row opens a clean one-page printable quote (letterhead, material line items, total) via the existing invoice-modal print pattern (`window.print()`), reusing the CSS already built for invoices.
   - **Print This List** button prints whatever's currently showing in the (filtered) All Quotes table as a summary table with a grand total.
   - A **Monthly Quote Report** card with a month picker: shows quote count / total quoted / won-landed count plus a summary table for that month, with its own Print button.
   - All three print views share the `#invoice-overlay`/`#invoice-modal` scaffold (a `.wide` class was added for the wider quote-list/report layout) — no new popups or CSS system.

## What just changed (session 3)
1. **QA pass fix** — a load saved with a blank Date field crashed the Dashboard and Weekly Invoice & Payroll tab entirely (`weekEndingFriday()`/`addDays()` called `.toISOString()` on an `Invalid Date` with no guard, and Date isn't a required field on the Load form). Both helpers now return `''` for a blank/invalid date instead of throwing, so a dateless load is just excluded from week/dashboard totals instead of breaking the page.
2. **Trucking → Sales progress tracking** — NFS Loads now has a **Material** field (text input with autocomplete from `distinctPitMaterials()`), so trucking can record what material each load hauled, not just qty/rate. Landed Jobs now shows a **Completed So Far** readout (both in the edit form and as a new "Completed" column in the All Landed Jobs table) that live-aggregates matching NFS Loads by **Job Name** (case-insensitive text match — same free-text field, no new dropdown) and **Material**, showing qty/unit/load-count and `% of planned` against that material's Qty Final (or Qty Requested if Final isn't set yet). This is read-only/computed, not stored — it never overwrites the sales side's own Qty Final field, so sales still finalizes/closes out a job manually. Loads with different units (e.g. tons vs yards) for the same material are kept as separate lines rather than summed, since the app doesn't do unit conversion.

## What just changed (session 4) — Supabase wiring, phase 1
1. **The app now reads/writes a real Supabase Postgres database** instead of `localStorage`. Project: `https://nujalvroqdjtbnqzxknq.supabase.co`. `supabase_schema.sql` (rebuilt from scratch this session — see its header comment for why) is the single source of truth for the DB structure; run it fresh if the project ever needs to be recreated.
2. **No login screen yet, on purpose** — every table currently has a `TEMP anon full access` policy so the app works with zero auth, matching today's actual security level (localStorage had none either). When the login screen goes in (phase 2), run `supabase_temp_open_access_REVERT.sql` to drop those temp policies — the real owner/sales_rep policies are already sitting underneath them, ready to take over.
3. **First load auto-seeds the database** — `loadState()` checks if the `pits` table is empty; if so, it pushes this app's built-in starter data (66 pits, compliance defaults, annual items) up to Supabase once. After that, Supabase is the source of truth and the in-code seed data is never referenced again.
4. **Schema note**: all record ids are `text` (not `uuid`) because the app generates its own ids as plain strings (e.g. `"m1k2j3xabcde"`) and the 66 built-in pits have hand-written ids like `"p-scc-samsula"`. Quotes and Landed Jobs each got a child table (`quote_materials`, `sales_job_materials`) since the original schema draft assumed one material per quote/job, but the app supports multiple line items per quote/job. `pit_materials` got a `kind` column (`'material'` vs `'dump_fee'`) to preserve the materials-vs-dump-fees distinction from an earlier session.
5. **Sync strategy**: `persist()` does a full upsert-and-prune sync of every table on every save (not a surgical diff) — simple and correct at this data scale (dozens–hundreds of rows), though not the most bandwidth-efficient approach if the dataset grows much larger later.
6. Verified end-to-end against the live database: writes, deletes, and nested child records (e.g. client contacts) all round-trip correctly; confirmed independently via direct REST calls, not just the app's own state.

## What just changed (session 5) — login + role-based access, phase 2
1. **Login screen added** — the whole app is now gated behind Supabase Auth (email/password). No public sign-up screen (deliberate choice): the owner creates each account manually via the Supabase dashboard (Authentication → Users → Add user), then sets that user's role in the `profiles` table (defaults to `sales_rep`; the owner flips their own row to `owner`).
2. **Sales rep nav is an allow-list, not a restrict-list** — `sales_rep` accounts only ever see three tabs: **Clients & Pits**, **NFS Sales Team**, **NFS Financials** (`ALLOWED_TABS_FOR_SALES_REP` in the code). Everything else (Dashboard, NNL/NFS Loads, Driver Compliance, Expenses, Invoices, Weekly Invoice & Payroll, Annual & Taxes, NNL Financials) is hidden from that role. Sales reps land on NFS Sales Team on login (Dashboard isn't in their allow-list). `owner` accounts still see every tab.
3. Verified in the browser (role simulated locally, not yet with a real second account): nav correctly shows only the 3 allowed tabs, all three render without errors, sign-out button and user badge (email + role) work.
4. **Still open**: the TEMP anon-access policies in `supabase_schema.sql` are still active, so the role restriction right now is UI-only — anyone with the anon key could still bypass it via direct API calls. Once the owner has logged in for real and confirmed everything works, run `supabase_temp_open_access_REVERT.sql` to drop those temp policies — the real owner/sales_rep RLS policies are already sitting underneath, ready to actually enforce this at the database level.
5. Not yet done: the owner hasn't created their real account yet (waiting on that before final end-to-end testing with real credentials).

## What just changed (session 6) — owner login confirmed, RLS recursion bug fixed
1. **Real owner account tested end-to-end for the first time** and hit a bug: after a successful Supabase Auth sign-in, the app fell back to `sales_rep` (only 3 tabs) instead of `owner`. Root cause was a Postgres error, not a role-assignment mistake: `42P17 - infinite recursion detected in policy for relation "profiles"`.
2. **Cause**: the `"owners can read all profiles"` RLS policy on the `profiles` table queried `profiles` from inside its own `USING` clause (`exists (select 1 from profiles p where p.id = auth.uid() and p.role = 'owner')`). Postgres RLS re-triggers the policy check on that inner query too, which recurses forever. The schema already had the correct fix pattern available (a `security definer` function, `is_owner()`) and used it for every other table — it just wasn't applied to this one policy on `profiles` itself.
3. **Fix**: `supabase_schema.sql` updated so `is_owner()` is defined first and the `"owners can read all profiles"` policy now uses `is_owner()` instead of a raw self-referencing subquery. A standalone non-destructive patch, `supabase_hotfix_profiles_rls.sql` (just drops + recreates that one policy, no data touched), was written and run directly against the live DB via the Supabase SQL editor — confirmed fixed by a direct authenticated REST call to `/rest/v1/profiles?select=role&id=eq.<uid>` returning `200 {"role":"owner"}` instead of `500`.
4. **Unrelated local-only snag while testing**: after applying the SQL fix, the Supabase JS client in the specific automated preview browser used for testing got wedged (`sbClient.auth.getSession()` hung indefinitely) — traced to a stale/wedged session token in that browser's `localStorage` after several rapid reloads during debugging, not a real app bug. Clearing `localStorage` and reloading fixed it immediately. Not expected to affect a normal user opening the app fresh in their own browser, but worth knowing if this ever recurs: clear the `sb-nujalvroqdjtbnqzxknq-auth-token` localStorage key and reload.
5. **Confirmed working end-to-end** with the real owner account: full nav (all 11 tabs) visible, user badge shows `nalunui21@gmail.com · Owner`, real data loads (Driver Compliance table populated and correct).
6. **Next step is now unblocked**: run `supabase_temp_open_access_REVERT.sql` to drop the temporary anon-access policies and actually lock the database down at the RLS level (still not done as of end of this session).

## What just changed (session 7) — self-service Change Password
1. **Added a "Change Password" button** in the sidebar next to Sign Out (`#changepw-btn`, [index.html:229](index.html:229)), for both owner and sales_rep accounts. Since the owner creates each sales team member's account manually in the Supabase dashboard with a password they pick, this lets each person change it to something only they know, without needing the owner to do anything.
2. **Implementation**: a small modal (`#password-overlay`, styled like the existing `#confirm-overlay` pattern) with New Password / Confirm New Password fields, wired to `sbClient.auth.updateUser({ password })` ([index.html:4737](index.html:4737) area) — the standard Supabase Auth call that updates the currently logged-in user's own password. Client-side validation: minimum 6 characters, must match confirmation. No admin/service-role access needed or used.
3. Verified working in a real browser (not the automated preview, which turned out to be sharing a browser tab/session with another concurrent Claude Code session working on the same project this session — caused a lot of confusing interleaved reload/login/logout noise in the network log while debugging; not an app bug, just a shared-tab artifact of testing two sessions against the same local server at once).

## What just changed (session 7, continued) — data-loss safety net
1. **Identified a structural data-loss risk**: every save (`persist()` → `syncStateToSupabase()`) does a full "upsert current in-memory state, then delete anything in the database not present in that in-memory copy" for every table (`deleteMissing()`, [index.html:1096](index.html:1096)), and for child records (contacts/materials on pits/clients/quotes/sales jobs) it's even more aggressive — `replaceChildren()` ([index.html:1106](index.html:1106)) unconditionally deletes *all* children of known parents and reinserts whatever the browser currently has in memory, on every single save. With 5 people now able to use the app, if any one browser tab's local copy is ever incomplete or stale relative to the database (slow network on load, two people editing at once, a tab left open across a schema change, etc.), the very next edit that person makes — anywhere in the app — would silently delete real data. This is exactly what looked like it happened during this session's testing (turned out to be a false alarm, confirmed via the Supabase Table Editor, but the underlying risk is real).
2. **Fix — safety circuit breaker**: `deleteMissing()` and `replaceChildren()` now refuse to proceed if the delete/replace would wipe out more than half of a table (only kicks in above 5 existing rows, to avoid false positives on small tables). Instead of deleting, they throw and show a persistent, dismissible red banner across the top of the app (`#safety-banner`, new element right after `<body>` so it's visible regardless of which tab/screen is active) explaining what was blocked and why, and that the fix is to reload the page for fresh data before making more changes (or, for a rare intentional bulk delete, to do it directly in the Supabase dashboard instead). Verified in the preview: the banner element renders hidden by default and toggles correctly.
3. **Still only a partial answer** — this catches the "silently wipes a large fraction of a table" failure mode, but does not replace real backups. The only backup mechanism today is the manual "💾 Backup Data" button on the Dashboard (downloads the full `state` object as a JSON file — nothing automatic, relies on someone remembering to click it and store the file somewhere safe). **Not yet done**: confirm what Supabase plan/tier this project is on and whether Point-in-Time Recovery or scheduled backups are enabled at the database level — that's the next real safety net to add, independent of anything the app itself does.

## What just changed (session 7, continued) — backup reminder
1. **Dashboard now nags about stale/missing backups.** Confirmed this project is on the Supabase **Free** tier, which has no automatic backups or Point-in-Time Recovery at all (those require Pro, ~$25/mo) — owner chose to stay on Free for now and rely on the existing manual "💾 Backup Data" button instead, so the gap is that nobody reliably remembers to click it.
2. **Fix**: clicking "Backup Data" now records a timestamp in `localStorage` (`jarelys_last_backup`). The Dashboard shows an amber reminder card (`renderBackupReminder()`, [index.html](index.html)) if it's been 7+ days since the last backup, or if one has never been downloaded at all. Verified all three states directly (never backed up / recent backup suppressed / 10-day-old backup shown).
3. **Known limitation**: the last-backup timestamp lives in that browser's `localStorage`, not in the database — so it's per-device/per-browser, not shared across whoever uses the app. If the owner backs up from a different computer, or clears browser data, the reminder resets and may nag sooner than it needs to. Not a data-safety issue (worst case is over-reminding), just worth knowing. If this becomes annoying, the fix would be to store the last-backup timestamp in a small Supabase table instead so it's shared across devices.
4. **This is still not a substitute for real backups** — it only reduces the odds of forgetting to click the button. Recommended next step whenever the budget allows: upgrade to Supabase Pro for actual managed daily backups, independent of anything the app does.

## What just changed (session 7, continued) — JSON backup respects connected folder
1. **"Backup Data" now uses the same folder-aware save logic as Excel exports.** Previously the JSON backup always went to the browser's default Downloads folder even if "📁 Connect Export Folder" had been used — only Excel exports respected that. Consolidated both into one shared helper, `writeBlobToFolderOrDownload()` ([index.html](index.html)), which `writeWorkbook()` (Excel) now calls internally too. If a folder is connected, the JSON backup writes straight there; otherwise it falls back to a normal download exactly as before. Verified: with no folder connected, the fallback download path still fires correctly (unchanged behavior); the folder-write path itself couldn't be exercised in the automated preview since picking a folder requires a real user click (File System Access API), but it reuses the exact code path already proven by Excel exports.

## What just changed (session 7, continued) — full QA click-through pass, no bugs found
Ran the full click-through pass called for in earlier sessions' "Known gaps," using the real live database (not a simulated/local copy), and cleaned up every test record afterward. Every tab tested add/edit/delete where applicable; zero console errors introduced across the entire pass.
1. **NNL/NFS Loads** — add, edit, delete all correct; the session-3 blank-date fix (`weekEndingFriday()`/`addDays()` returning `''` instead of throwing) is still working: a dateless load saved fine, was correctly excluded from "This Week's Revenue" but correctly included in "Total Revenue (All Time)," and the Dashboard didn't crash. NNL driver pay auto-calculates at 30%. NFS Material field works.
2. **Driver Compliance** — add/edit/delete correct; the 30-day "Expiring Soon" threshold ([index.html:2143](index.html:2143)) fires correctly; status filter dropdown (All/Complete/Issues/Expiring) works.
3. **Expenses** — add/edit/delete correct (found via `data-edit-exp`/`data-del-exp` attributes, not by button text — a maintenance note for next time, since the Truck Maintenance section on the same tab reuses similar-looking rows without those buttons).
4. **Invoices + Weekly Invoice & Payroll** — generated a real invoice end-to-end: sequential invoice numbering incremented correctly (`NNL-1002`), correct week-ending date range, correct line-item math, load's `status` auto-flipped to "Invoiced" and `invoiceNum` got stamped, and the new invoice immediately showed up in the "All Invoices" list. Weekly tab correctly grouped the same load by broker and driver.
5. **Clients & Pits** — Contacts add/delete correct. Confirmed the session-2 B&B Pits fix is still intact in the live DB: named "B&B Pits" (no Calhoun affiliation), pricing correctly under `materials` not `dumpFees`. Pit count still exactly 66.
6. **NFS Sales Team — Quotes** — logging a quote with a material line item correctly auto-populates tax rate from the pit, and profit auto-calculates at 18% of (material cost + trucking cost); manual override still works if typed.
7. **NFS Sales Team — Landed Jobs "Completed So Far"** — cross-tab aggregation (session 3 feature) verified working end-to-end with real data: an NFS load with Job Name "QA Test Job" / Material "#57 Stone" / qty 18 was correctly picked up by a Landed Job with the same Job Name, showing "#57 Stone: 18 ton(s) across 1 load (36% of 50 planned)" — exact match on the expected math.
8. **Annual & Taxes** — inline add/edit/delete of annual items works; this table deletes immediately with no confirmation dialog (unlike Loads/Compliance/Expenses, which all confirm first) — a minor UX inconsistency, not a bug, not fixed since it wasn't asked for.
9. **NNL/NFS Financials** — both render without errors.
10. **sales_rep role restriction** — re-verified the nav allow-list still works correctly (only Clients & Pits / NFS Sales Team / NFS Financials show, auto-redirects off Dashboard) after the session-6 RLS lockdown; done via a client-side role simulation rather than a real second login, consistent with how this was verified in session 5, since real sales-rep credentials weren't necessary to re-confirm the UI-layer logic (the RLS enforcement itself was already independently verified via direct REST calls in session 6).
11. **One quirk encountered, not a bug**: mid-pass, the visible tab changed out from under the automated test twice, and the underlying JS draft state stayed intact both times. Root cause: the browser tab being driven by the automated preview tooling is the same tab the owner can see/use live, so real navigation by the owner shows up mid-test. Not a defect — just something to know if this pass is ever repeated: expect this if the owner is also looking at the app at the same time.

**No bugs found. No code changes were needed as a result of this pass.**

## What just changed (session 8) — file attachments for Contacts, Pits, and Driver Compliance
1. **Renamed the top-level "Clients & Pits" tab to "Contacts and Pits"** (nav label, page heading, and one internal alert message that referenced the old name). The "Contacts" subtab label itself was already correct from session 2.
2. **Added file upload/storage** to Contacts, Pits, and Driver Compliance — manual entry only, no AI auto-read. The owner considered auto-extracting data from photos (price sheets, business cards, compliance docs) but decided against it: it needs a funded Anthropic API key, and the existing "Scan Ticket Photo" feature on NNL/NFS Loads was discovered to have the same requirement — it calls `api.anthropic.com` directly from the browser with **no API key set at all** ([index.html:1574](index.html:1574) area), so it likely doesn't work today outside a sandboxed preview. That's a pre-existing gap, not something this session touched — worth fixing if the owner ever wants that feature to actually work.
3. **New schema**: `client_attachments`, `pit_attachments`, `compliance_attachments` — one child table per parent, matching the existing `client_contacts`/`pit_contacts` pattern rather than a single generic table, so it slots into the same conventions a future reader already knows. `compliance_attachments` is **owner-only** (same `is_owner()` RLS policy as the `compliance` table itself) — sales reps must never see driver compliance documents, matching the existing business rule. Migration: [supabase_attachments_migration.sql](supabase_attachments_migration.sql) (already run against the live DB); `supabase_schema.sql` updated to match for any future full rebuild.
4. **New Supabase Storage bucket** `attachments` (private — not publicly readable; only reachable through the app with an authenticated session via signed URLs, 5-minute expiry). Objects are stored at `<client|pit|compliance>/<parent_id>/<uuid>-<filename>`, and storage RLS policies read the entity type straight off that path prefix via `storage.foldername()`, mirroring the same owner-only-for-compliance split as the DB tables.
5. **Architecture note**: unlike every other child table in this app, attachments are **not** synced through the routine `persist()`/`replaceChildren()` full-array cycle. Uploads and deletes happen immediately, one file at a time, directly against Storage + their table the moment the user acts. Folding them into the routine sync would mean every single unrelated edit anywhere in the app re-deletes-and-reinserts every attachment row on every save — wasteful, and it would repeatedly re-trigger the prune safety check (from earlier this session) for no reason. `fetchStateFromSupabase()` still loads them once at boot into `state.clients[i].attachments` / `state.pits[i].attachments` / `state.compliance[i].attachments` so the UI can display them normally.
6. **Client-side image compression before upload**: photos are resized to a max 1600px dimension and re-encoded as JPEG at 80% quality (`compressImageFile()`, [index.html](index.html)) before hitting Storage — verified in testing that an 80KB/2000×1500 test image compressed to ~22KB. PDFs pass through unchanged. This matters because Supabase's Free tier only includes 1GB of storage with no automatic overage; compressing images stretches that considerably further. Multiple files per record are supported and kept (no "replaces the old one" behavior), per the owner's preference to keep file history (e.g. last year's AND this year's insurance cert).
7. **Verified end-to-end against the live database and live storage bucket** (not just app state): uploaded a test image to a real pit — confirmed the row landed in `pit_attachments` and the compressed file landed in the real Storage bucket; generated a signed URL and confirmed it actually fetches the image (200, correct content-type); deleted it and confirmed both the DB row and the storage object were gone. Repeated the same for a compliance entry, plus confirmed a PDF upload passes through uncompressed with its extension intact. Confirmed anonymous access to `compliance_attachments` returns zero rows — the owner-only restriction relies on the exact same `is_owner()` policy already proven correct for the `compliance` table itself earlier this session, so it's expected to correctly block sales_rep accounts too by the same mechanism (not separately re-verified with a real second account, consistent with how the sales_rep nav-restriction re-check was handled).
8. **Cost note given to the owner**: Supabase Storage Free tier = 1GB included, no overage without upgrading to Pro (which bundles the backup features discussed earlier this session too). Anthropic API pricing (if the owner ever revisits auto-read) was checked live rather than assumed from training data — current Claude Sonnet 5 introductory pricing is $2/$10 per MTok input/output through Aug 31 2026 ($3/$15 after), and Claude Haiku 4.5 ($1/$5 per MTok) was recommended as the better fit for simple field-extraction tasks like this, both cheaper than what's hardcoded in the existing (non-functional) ticket scanner.

## What just changed (session 9) — Pit Map reliability for heavy use
The owner wants to rely on the Pit Map a lot, so this session focused on de-risking its one real single point of failure: geocoding.
1. **Switched job/pit address geocoding from OpenStreetMap's Nominatim to OpenRouteService's own Geocoding endpoint** (`geocodeAddress()`, [index.html:2606](index.html:2606)) — same API key already used for routing, same account/quota, and actually intended for this kind of app usage. Nominatim is a free community resource with a strict "not for automated/production use" policy and no paid tier to buy more headroom; repeated automated use risked getting silently blocked with no recourse. Verified live before switching: for a well-known address both providers returned matching coordinates; for a sparser rural/industrial address (typical of pit addresses), Nominatim returned nothing at all while ORS gracefully fell back to city-level accuracy — so this is a strict improvement, not just a lateral move.
2. **Checked current OpenRouteService free-tier limits directly** rather than assuming: roughly 2,500 requests/day, 40,000/month (worth confirming in your own ORS dashboard, since the official page didn't fully render for an automated check). Comfortable headroom for a small team's daily use, but it's one shared key across everyone using the app — worth knowing if usage ever grows a lot.
3. **Fixed a misleading error message**: the distance-lookup call used to say "double check your API key below" on *any* failure, including a rate-limit hit. It now distinguishes a 429 (rate/daily limit — explains the key is shared across the whole team) from a 401/403 (actually-bad key) from other failures.
4. **`geocodeAddress()` now requires an ORS key** (previously worked without one, since Nominatim needs no key) — in practice this doesn't change anything for real users since the app already bakes in a default shared key ([index.html:842](index.html:842)), but it's a behavior change worth knowing if that default is ever cleared via "Change key" without entering a replacement.
5. **Adjusted the bulk "Locate pits on map" throttle** from 1.1s/request (matching Nominatim's 1 req/sec policy) to 1.6s/request (comfortably under ORS's 40 req/min limit).
6. **Verified end-to-end against the live ORS API** (not mocked): confirmed the exact request/response format via direct curl calls first, then ran the full geocode-address → matrix-distance pipeline through the app's real code path with a live pit and real API key — got back a correct, realistic result (21.5 mi / ~34 min, downtown Orlando to Apopka). Confirmed the test coordinates used for this never touched the live database (checked directly — still `null` there, exactly as the owner left it) and reset the in-memory test state afterward.
7. **Note for later**: as of this session, 0 of the 66 pits have been geocoded yet in the live database (discovered while testing) — the owner will need to click "Locate pits on map" at some point before the map/search features have real located pits to work with. Not something this session needed to do, just worth flagging so it isn't a surprise.

## What just changed (session 9, continued) — Fill Dirt naming cleanup
1. **Data cleanup, not a code change**: found 5 name variants across pit materials — "Fill Dirt", "Fill Dirt - General", "General Fill Dirt", "Structural Fill Dirt", and "Unscreened Top Soil/Fill Dirt". Owner confirmed only the first three are truly the same product; "Structural Fill Dirt" and "Unscreened Top Soil/Fill Dirt" are genuinely different products (different specs/pricing) and were deliberately left alone.
2. Renamed "Fill Dirt - General" (Space Coast Crushers) and "General Fill Dirt" (Platt Ranch) to plain "Fill Dirt" — 2 rows total. Applied through the app's own state + `persist()`/`syncStateToSupabase()` path (not a raw SQL update), so it went through the normal sync/safety-check code exactly like a real edit through the UI would. Verified: both rows show "Fill Dirt" in the live database afterward, and every other table's row count was re-checked and confirmed unchanged (pits still 66, pit_materials still 457, etc.) — since a full sync touches every table, not just the one being edited.

## What just changed (session 9, continued) — geocoding fallback + a real OpenRouteService outage found
1. **The owner hit "could not locate that address" on the Pit Map right after this session's ORS switch.** Investigated and confirmed it was **not a bug** — OpenRouteService's API server was refusing connections at the raw network level (`ERR_CONNECTION_REFUSED`, confirmed independently via a direct `curl`/TCP test outside the app entirely, unaffected by anything in this codebase). Their main website and DNS were both fine; only the dedicated API server was unreachable. Timing note: this surfaced right after both this session's testing and the owner's own bulk "Locate pits on map" attempts sent OpenRouteService a burst of requests — it's possible (not confirmed) that contributed to a connection-level block rather than a normal rate-limit response; could also just be a plain outage. Not something fixable from this side either way.
2. **Added a fallback to Nominatim** in `geocodeAddress()` ([index.html:2606](index.html:2606)): OpenRouteService is still tried first (better quota, meant for this kind of use), but if it fails for *any* reason — including being completely unreachable — it now falls through automatically to the old Nominatim lookup instead of just giving up. This directly addresses the tradeoff called out earlier this session: consolidating geocoding onto one provider means an outage there no longer takes down the whole Pit Map.
3. **Verified against the live (still-down) OpenRouteService outage**: called `geocodeAddress()` for a real address and confirmed via the network log that the ORS attempt failed exactly as expected, immediately followed by a successful Nominatim call for the same address, returning the correct coordinates in well under a second (the ORS failure is near-instant — a connection refusal, not a hang — so the fallback adds negligible delay).

## What just changed (session 10) — stuck-login watchdog
1. **The recurring "stuck on Loading…" issue finally got a real fix.** This session (and several times in earlier sessions) hit a case where Supabase's auth client gets wedged — sometimes at `getSession()`, this time specifically at the profile-role fetch inside the `onAuthStateChange` callback — and the boot screen just sits on "Loading…" forever with zero feedback. The only previous fix was knowing to open DevTools and run `localStorage.clear()`, which isn't realistic for the owner or any sales rep to know.
2. **Added a watchdog** ([index.html:214](index.html:214) for the UI, [index.html:5011](index.html:5011) for the logic): if the boot sequence hasn't finished within 8 seconds, a "This is taking longer than it should" message and a **Reset & Reload** button appear automatically. Clicking it clears just the Supabase session key from `localStorage` and reloads — the one-click equivalent of the old manual DevTools fix, no technical knowledge required.
3. **Fixed a bug in my own first draft of this**: the watchdog's "resolved" flag was originally set at the very top of the `onAuthStateChange` callback, before the async profile-role fetch — so if *that* step hung (as it did during this session's testing), the watchdog would wrongly think everything was fine and never show the button. Moved the flag to fire only once the boot screen is actually dismissed (after `showApp()`/`showLoginScreen()`), so it correctly catches a hang anywhere in the chain, not just a totally silent callback.
4. **Verified both directions**: confirmed the watchdog does *not* show up on a normal successful login (no false positive), and confirmed the actual button logic removes the stale session key and reloads correctly (verified by calling the handler directly, since the automated preview browser's synthetic click had an unrelated quirk in this specific shared-tab testing session — the underlying code was checked twice independently and is correct).

## What just changed (session 11) — geocoded all pits, QA pass, ticket scanner removed, backups deferred again
1. **All 66 pits are now geocoded on the live database.** Clicked "Locate pits on map" on the Pit Map tab; the run confirmed OpenRouteService was actually down again this session (`ERR_CONNECTION_REFUSED` on every request) and every single pit fell through to the Nominatim fallback added last session — real-world proof that fallback works end-to-end, not just in a synthetic test. Ran the button twice (first pass got 9/23, second pass mopped up the rest — normal behavior, not a bug, since each click only retries pits still missing coordinates). The Pit Map now shows "All pits are located."
2. **Full QA pass on the newer features, all clean, no bugs found:**
   - File attachments on **Pits** and **Driver Compliance**: full upload → auto-compress-to-JPEG → signed-URL view → delete cycle tested against the live Storage bucket and DB tables, verified clean afterward (no leftover test rows/files in either).
   - **Pit Map & Routing**: ran a real "Find Nearby Pits" search, confirmed round-trip distance/time sorting, the `Compare` rate calculator (per-load/per-ton/per-hour/per-mile with the $2/mile floor and "✓ best" highlighting), and "Fill Dirt" vs "Unscreened Top Soil/Fill Dirt" staying correctly distinct per the naming rule below.
3. **Removed the ticket scanner feature entirely** (`📷 Scan Ticket Photo(s)` on NNL/NFS Loads) rather than trying to fix it. Confirmed it was more broken than previously known: it's not just a missing API key — the code calls `api.anthropic.com` directly from the browser with no auth header, which fails outright on CORS (`Failed to fetch`) before an API key would even matter. A real fix would need a backend proxy (e.g. a Supabase Edge Function) to hold the key server-side; owner chose to just remove the dead feature instead of building that. Deleted the button, hidden file input, status text, and all now-unused JS (`extractTicketFromImage`, `processScannedTickets`, `addExtractedTicketRow`, `fileToBase64`, `TICKET_EXTRACT_PROMPT`) — manual ticket entry is now the only path, with nothing left in the UI suggesting otherwise. Verified both NNL and NFS Loads render cleanly post-removal with no console errors.
4. **Real database backups (pg_dump) deferred again.** Neither Homebrew nor Postgres.app is installed on this machine, so `backup_database.sh` still can't run. Owner chose to stick with the existing manual "💾 Backup Data" JSON export + the Dashboard's 7-day nag rather than install anything or upgrade to Supabase Pro right now. Revisit whenever it feels worth the setup/cost.
5. **Unrelated hiccup during this session, not a real bug**: the Supabase auth client hit the same known intermittent "stuck on Loading…" issue described in session 10 (this time on `getSession()` after a page reload). The session-10 watchdog and its "Reset & Reload" button worked exactly as designed — one click cleared the stale session key and the app booted clean on the next sign-in.

## Known gaps carried over from session 2
- The Downloads copy of this project (`nalu-command-center 2`) was **not** updated to match — only `/Users/nalunui/Desktop/Claude/nalu-command-center` has these fixes. If that copy is still in use, it needs the same edits or should be treated as stale.
- Users who already have this app saved with old B&B pit / Clients data in their browser's localStorage won't see the corrected seed data automatically — `state.pits` (unlike `orsApiKey`) is fully replaced by whatever was previously saved, so a fresh browser profile (or a manual data reset) is the only way to pick up the corrected B&B records if someone already has old data persisted.

## Known gaps / what's NOT done yet
(This section predates Supabase/login/RLS — those are all long since done. What's actually still open as of session 11:)
- **Real database-level backups (pg_dump) not set up.** `backup_database.sh` exists and is correct, but neither Homebrew nor Postgres.app is installed on this machine, so it can't run. Owner has deferred this twice now; relying on the manual "💾 Backup Data" JSON export + Dashboard's 7-day nag in the meantime. Supabase Pro ($25/mo) is the no-install alternative (bundles managed daily backups + 100GB storage).
- **OpenRouteService key is a personal free-tier key**, baked in as the shared default for all users. Generous quota (~2,500 req/day) but worth knowing if usage ever grows a lot, and it did have a real outage this session (confirmed the Nominatim fallback handled it correctly).
- **The Downloads copy of this project was stale** as of session 2 — see below; has since been deleted per an earlier session's changelog, so no longer a live concern, just noted for history.

## Suggested next steps, in order
1. **Decide on real database backups** — install Homebrew/Postgres.app to run the existing `backup_database.sh`, or upgrade to Supabase Pro, whenever it feels worth the cost/setup.
2. **Full bug/QA pass covering everything, not just the newer features** — session 11 verified attachments (Pits/Compliance) and Pit Map & Routing end-to-end with no bugs found; NNL/NFS Loads, Expenses, Invoices, Weekly Invoice & Payroll, Annual & Taxes, and the financial report tabs haven't had a dedicated pass in a while.
3. **Start logging real loads/data** — as of session 11 the live database has 66 real pits (all geocoded) and 4 real Driver Compliance records, but 0 loads, 0 expenses, 0 contacts, 0 quotes — the app is fully wired and tested but not yet in daily use.

## Business rules to preserve (came from earlier discussion, not in code comments)
- Truck pay should never fall below **$2/mile** on any calculated rate — the Pit Map rate calculator already enforces this with a visible warning.
- Sales team should have **full edit access** to: NFS loads/financials, expenses, annual items, clients, pits, quotes, sales jobs.
- Sales team should have **zero visibility** into: NNL loads, driver pay, and the driver compliance tab — this must be enforced at the database level (RLS), not just hidden in the UI, per `supabase_schema.sql`.
